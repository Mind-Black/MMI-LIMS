# Tool Images

Place tool images in this directory.
The naming convention is `[tool_id].jpg` (or png, etc.).

For example, if a tool has ID `1`, the image should be `1.jpg`.

The application logic currently uses a placeholder but can be updated to dynamically load these images based on the ID.
